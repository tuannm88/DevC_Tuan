#include <iostream>
#include <math.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) {
	int a, b;
	printf("nhap vao so a = ");
	scanf("%d",&a);

	b=pow(a,2);
	printf("binh phuong cua a = b = %d",b);
	return 0;
}
